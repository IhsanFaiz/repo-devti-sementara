import { Stack } from '@mui/material';
import ProjectTableServer from 'components/table/ProjectTableServer';

const Page = () => {
  return (
    <Stack spacing={2}>
      <ProjectTableServer />
    </Stack>
  );
};

export default Page;
