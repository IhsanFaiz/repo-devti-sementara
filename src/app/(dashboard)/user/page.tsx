import { Stack } from '@mui/material';
import UserTable from 'components/table/UserTable';

const Page = () => {
  return (
    <Stack spacing={2}>
      <UserTable />
    </Stack>
  );
};

export default Page;
