import { Stack } from '@mui/material';
import OnboardingTable from 'components/table/OnboardingTable';

const Page = () => {
  return (
    <Stack spacing={2}>
      <OnboardingTable />
    </Stack>
  );
};

export default Page;
