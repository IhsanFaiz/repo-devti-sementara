'use client';

import { Stack } from '@mui/material';
import SelectionTable from 'components/table/SelectionTable';

const Page = () => {
  return (
    <Stack spacing={2}>
      <SelectionTable />
    </Stack>
  );
};

export default Page;
