import { Stack } from '@mui/material';
import RequestTableServer from 'components/table/RequestTable';

const Page = () => {
  return (
    <Stack spacing={2}>
      <RequestTableServer />
    </Stack>
  );
};

export default Page;
